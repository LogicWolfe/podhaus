# Alligator → Bilby Migration

Migration of all running services from `alligator` (Intel NUC, x86_64, Linux) to `bilby` (Apple Silicon M1 Mac mini running Asahi Linux, aarch64). Bilby is the new home of the `podhaus` deployment. Once complete, alligator is retired.

## Resumption pointer (read this first if returning to a fresh session)

**Current state (2026-04-16):** phases 1–5a, 6 (architecture pivot), 6 (arm64 spot-check), **7 (all stack migrations)**, **8 (tunnel cutover)**, and **12 (retire alligator)** are complete. **Every Komodo-managed stack runs on bilby. Alligator is powered off, its DNS A record removed.** Plex (standalone, non-Komodo) also on bilby. **Backrest config-as-code is live with 9 nightly plans covering every stack with state; first snapshots of the entire deployment taken (14.733 GiB across 9 snapshots, 12 GB on-disk in the Pouch repo).** Original `/Jump/plex` (102 GB) still intact on the NAS as a safety net.

**Design principles (canonical, post-pivot):**

- **Sizing rule:** local NVMe <5 GB, consult 5–50 GB, Pouch ≥50 GB. Jump is **backup target only**.
- **Config-as-code:** any config that *can* live in the repo, *should*. Secrets live in 1Password and are templated in at deploy time via `komodo-op` → Komodo Variables → `${VAR}`. Container-volume-only state is not an acceptable substitute — those files get lost on rebuild.
- **Bilby has persistent local state.** The original "stateless bilby, all state on NFS" principle was abandoned in the phase 6 pivot. Bilby is now the source of truth for all container state; backups are the durability mechanism.
- **Backrest → local restic repo on Pouch → rclone sync to OneDrive.** Restore drill run once (Preferences.xml), repo encrypted with password from 1P. Plans + hooks + notifications are still a pending follow-up (phase 6.5).

**Next action when resuming:**

1. **Remaining phase 6.5 items**: Victoria Logs swap, Grafana dashboards. (Backrest config-as-code, rclone.conf writable mount, rclone config-as-code split, Plex `/transcode` fix, ofelia + Plex stats cleanup all **done** as of 2026-04-16.)
3. **Phase 5b** (Kuma state migration from Railway) — Railway CLI dance.
4. **Phase 9** (OneNote export resume — catalog.sqlite already saved to `/home/nathan/repos/podhaus-migration-state/onenote-exporter/cache/db/` before alligator powered off).
5. **Phase 11** (doggos/yiayia Railway migration — services unknown, survey first).
6. **Phase 13** (README rewrite — end-of-project housekeeping).
7. **Syncthing relocation to NAS host** — deferred, separate work from the podhaus migration.

**Credentials status:** `railway-api-token`, `postmark-smtp`, `restic-repo-password`, `rclone-onedrive-token` — all four in 1P Homelab, surfaced as Komodo Variables via komodo-op.

## Environment-specific workarounds

All reversible, all documented in the affected compose files with inline comments.

- **NAS `/Jump` squash** — kangaroo's `/Jump` export squashes all writes to uid 1000:gid 100 and denies chown, so container images that chown their data dir on startup fail to deploy against Docker NFS volumes. Originally hit by `ghcr.io/ferretdb/postgres-documentdb`, `postgres:16`, and `1password/connect-api`. Affected volumes were carved out to local storage as "temporary" exceptions — the phase 6 pivot made those carve-outs permanent via the sizing rule, so this is no longer a blocker.
- **SELinux enforcing on Fedora Asahi** — denies container access to `/var/run/docker.sock` and cross-label reads into bind-mounted config files. Applied `security_opt: label:disable` to `komodo-core`, `komodo-periphery`, `autoheal`, `backrest`, `loki`, `alloy`, `grafana`, `uptime-kuma`. Also use `:z` flag on host bind mounts that need container write access (see `plex/compose.yaml`, `flood/compose.yaml`).
- **`ghcr.io/0dragosh/komodo-op` mislabelled arm64 manifest** — upstream Dockerfile hardcodes `--platform=linux/amd64` + `GOARCH=amd64`, so the "arm64" tag is actually an amd64 image that crashes with exec format error on real aarch64. Built a local arm64 image from upstream source via `onepassword/komodo-op.Dockerfile`. Compose pins `image: komodo-op:local-arm64` with `pull_policy: never`. Upstream fix deferred (noted in Deferred).
- **Multi-line 1P Secure Notes don't round-trip through Komodo env files** (truncation at the first newline) — `komodo-start` renders `rclone.conf` on the host via `op CLI` and stacks bind-mount `/etc/komodo/rclone/rclone.conf` directly. Re-run `komodo-start` to refresh after OneDrive token rotations.
- **`cloudflare/cloudflared:latest` is distroless** — no shell, no wget, no curl, no busybox, only the `cloudflared` binary. In-container healthchecks that touch `/ready` on port 20241 are structurally impossible. Dropped the healthcheck and autoheal label from `cloudflare-tunnel/compose.yaml` (commit `3bba849`). cloudflared handles its own reconnection internally; `restart: unless-stopped` handles outright crashes; external liveness will be monitored via Uptime Kuma hitting the public hostnames.
- **Komodo ResourceSync does not apply `[[variable]]` entries from variable TOML files** — see the "Known gotcha: Komodo variable seeding" section below. Fixed 2026-04-14 via commit `336b961` which adds `TZ` and `MEDIA_DIR` to `komodo-start`'s `seed_variable` loop.
- **Komodo first-boot chicken-and-egg** — `komodo-start` reads a pre-existing Komodo API key from 1P, but on a fresh DB no such key exists. Manual workaround documented in the progress log; folding into `komodo-start` as an automatic first-boot path is deferred.
- **Bind mount paths are host-relative, not periphery-relative** — Komodo-managed stack `run_directory` paths (`/etc/komodo/repo/<stack>`) exist inside periphery but not on the host. Docker daemon resolves bind mount sources from the host filesystem, so relative paths in compose (`./loki-config.yaml`) resolve to non-existent host paths and Docker silently creates empty stub dirs. Fix: use absolute host paths in any compose file that bind-mounts repo-resident files.

## Known gotcha: Komodo variable seeding for non-secret variables — FIXED 2026-04-14

**Problem:** `komodo/sync/variables.toml` declares `TZ` and `MEDIA_DIR` as `[[variable]]` entries, and Komodo's ResourceSync *reads* them as remote content — they're visible in the `remote_contents` field of `GetResourceSync`. But the sync never materialized them as live Komodo Variables: `variable_updates` in the sync result stayed empty. Stacks that reference `[[TZ]]` or `[[MEDIA_DIR]]` in their `stack.toml` environment blocks got literal unresolved `[[TZ]]` written into their `.env` files on redeploy.

**How it manifested:** At original-deploy time the variables existed (manually seeded once, long ago), so each stack's `.env` was populated correctly and the running containers all got `TZ=Australia/Perth`. Later the variables were cleared (cause unknown). Subsequent `./komodo-sync` runs rewrote the `.env` files with `TZ=[[TZ]]` literal, but docker-compose didn't recreate containers when the spec hadn't changed, so the divergence was invisible until a real redeploy. Audit 2026-04-14 found `logging`, `uptime-kuma`, and `backup` in this state.

**Fix (landed in commit `336b961`):** `komodo-start` now calls `seed_variable` for `TZ=Australia/Perth` and `MEDIA_DIR=/mnt/pouch` the same way it seeds the 1P credentials. `variables.toml` stays around as a declarative reference but is no longer the authoritative source. `seed_variable` grew an `is_secret` parameter (default `true` to preserve existing behavior; TZ and MEDIA_DIR pass `false`). Affected stacks were redeployed via Komodo API to rewrite their `.env` files with correct interpolation — no container recreation was needed because the compose specs hadn't changed and the running containers already had the correct env vars from the original deploys.

**Flood was also backfilled:** `flood/stack.toml` now references `TZ=[[TZ]]` and `flood/compose.yaml` sets `TZ: ${TZ}` in the environment block. Flood previously had no TZ env var set at all.

## Context

- **alligator**: i5-8259U, 8 threads, 7.6 GB RAM. Running home-assistant, paperless, its own Komodo Core, and 1Password Connect. Swapping heavily.
- **bilby**: Apple M1, 8 cores, 15 GB RAM, arm64. Running the full podhaus deployment now except HA and paperless.
- Both machines mount the kangaroo NAS (`10.0.0.25`) — alligator at `/mnt/NFSPouch`, bilby at `/mnt/pouch`.
- **kangaroo NAS storage layers**: Pouch is on 5 spinning HDDs (29 TB, ~5.2 TB free). Jump is on 2 SATA SSDs (382 GB total, ~280 GB free). Equal throughput (CPU-bound on NFS controller), but Jump has dramatically better IOPS. Post-pivot: Jump is effectively reserved as a backup target; all primary state is on bilby local NVMe per the sizing rule.

## Completed phases (summary)

- **Phase 1 — Pre-flight cleanup.** Compose files for flood / home-assistant / paperless / onepassword / komodo (bootstrap) / autoheal / cloudflare-tunnel updated. Autoheal promoted to a proper Komodo stack. Paperless scripts pulled from alligator into this repo. Docker daemon log-driver caps (`local`, `max-size=50m`, `max-file=3`) applied on bilby. Committed as `d2d7346`.
- **Phase 2 — Komodo bootstrap.** komodo-core, komodo-postgres, komodo-ferretdb, komodo-periphery, onepassword stack (op-connect-api, op-connect-sync, komodo-op), autoheal all deployed and healthy on bilby. 28 Komodo Variables populated by komodo-op from the 1P Homelab vault. Env-specific workarounds (NAS squash, SELinux, komodo-op arm64, multi-line secrets, first-boot API key) documented above.
- **Phase 3 — Backup infrastructure (partial).** Backrest deployed. Restic repo `857078229d` initialised at `/mnt/pouch/backups/` with encryption password from 1P. First snapshot `866cbd66` taken (100.966 GiB source / 17.521 GiB stored). Restore drill against `Preferences.xml` matched source bit-for-bit. `rclone sync` to `onedrive:Backups/podhaus-restic/` completed (17.526 GiB / 1105 files). **Backup gate satisfied.** Plans + hooks + notifications + scheduling all deferred to phase 6.5 (`backup/config.json.tmpl`). plex-backup container retired early in phase 2 because its project-default network was squatting on `172.18.0.0/16` and blocking `dockernet` creation.
- **Phase 4 — Logging infrastructure (partial).** Loki + Grafana Alloy + Grafana deployed and healthy. Alloy scrapes all local containers via docker socket; logs flowing. Grafana exposed via `logs.pod.haus` ingress rule. Loki → Victoria Logs swap pending (phase 6.5). Dashboards deferred until after the swap.
- **Phase 5a — Uptime Kuma scaffolding.** Fresh empty Kuma instance on bilby, state in local volume, port 3001, `uptime.pod.haus` ingress staged. Railway state migration is phase 5b (still pending).
- **Phase 6 — Architecture pivot (2026-04-12/13).** A Plex client-visible outage escalated into a multi-day debugging session. Root causes, in order of discovery: wrong host timezone (butler running at peak hours), missing theme-music DB records, and the real killer — the 47 GB Plex DB was 99.6% bloat in the `statistics_bandwidth` table (796M rows, 9 years of history, no upstream pruning mechanism). Offline table rebuild cut it to 3,405 rows; followup VACUUM completed in ~60s and the DB went 47 GB → 217 MB. Plex rebuilt from a fresh `rsync /Jump/plex` onto local NVMe at `/var/lib/plex-local`; BIFs (183 GB on Pouch) switched from in-config symlink to explicit bind mount. **Design principle changed from "stateless bilby on NFS" to "local state is the default."** Sizing rule locked in. Lessons carried forward: SQLite on NFS is fragile under write contention; Plex has no stats pruning (we own it now — phase 6.5 Ofelia job); Plex "force refresh" is destructive to user poster selections; autoheal + long DB ops is a bad combination; treat originals as sacrosanct when debugging; byte math > `du -sh` math. Full historical detail preserved in the progress log.
- **Phase 6 (spot-check) — Arm64 compatibility.** Every image except `ghcr.io/0dragosh/komodo-op` runs cleanly on arm64 as-is. komodo-op worked around with a locally-built image. Full list: `jesec/rtorrent-flood`, `ghcr.io/ferretdb/postgres-documentdb`, `1password/connect-api`+`connect-sync`, `garethgeorge/backrest`, `grafana/loki`+`alloy`+`grafana`, `louislam/uptime-kuma`, `willfarrell/autoheal`, `cloudflare/cloudflared`.
- **Phase 8 — Cloudflare tunnel cutover (2026-04-14).** Executed alongside the flood migration rather than after all three stack migrations. Alligator was briefly offline earlier in the day which simplified coordination — no hot swap needed, just "stop on alligator, start on bilby." Bilby's cloudflared registered 4 tunnel connections (2× mel02, 2× per01). `https://komodo.pod.haus` returns 200, `https://torrent.pod.haus` 302s to Cloudflare Access. `sync.pod.haus` / `home.pod.haus` / `paperless.pod.haus` ingress rules deliberately left in place (will return 502 until HA + paperless migrate); `logs.pod.haus` / `uptime.pod.haus` ingress rules live in the tunnel config but CNAMEs for those hostnames are missing from `dns/dnsconfig.js` — needs dns-push when external access to Grafana/Kuma is wanted. Commits `46a0113`, `3bba849`.

## Active work

### Phase 5b: Uptime Kuma state migration from Railway

Railway state migration + cutover, user-driven because it involves Railway CLI auth and stopping the live Railway instance.

- [ ] Install `railway` CLI on bilby (or run via container)
- [ ] Authenticate with `railway-api-token` from 1Password
- [ ] Identify the Kuma project + service + volume in Railway
- [ ] Stop Railway Kuma briefly OR use `sqlite3 .backup` for consistent live copy
- [ ] `railway ssh` into Kuma container, `sqlite3 /app/data/kuma.db .backup /tmp/kuma-snapshot.db`, copy it out
- [ ] Copy `kuma.db` and any other `/app/data` contents into the bilby Kuma's `uptime-kuma-data` volume
- [ ] Verify all monitors, notification configs, users restored correctly
- [ ] Update `dns/dnsconfig.js` — remove the Railway CNAME for `uptime`, let the tunnel handle it
- [ ] `dns-push` to apply
- [ ] Add Docker container monitors in Kuma for each existing bilby container
- [ ] Add HTTP monitors for every `*.pod.haus` endpoint
- [ ] Wire Backrest push-monitor URL into backup hooks (Kuma alerts on missing heartbeat)
- [ ] Decommission Railway Kuma project (keep for 72h as rollback safety, then delete)

### Phase 6.5: Post-recovery follow-ups (phase 6 pivot aftermath)

Work that falls out of the architecture pivot. Do before finishing phase 7 so the patterns (local storage, config-as-code, backrest plans) are in place when each new stack lands.

- [x] **Backrest compose + config-as-code** — done 2026-04-16. `backup/compose.yaml` rewritten: dropped the dead `plex_jump` external volume, bind-mounted every migrated stack's state dir read-only at `/userdata/<stack>` or `/userdata/<stack>/<subdir>`, added `external: true` refs for the docker named volumes owned by komodo/onepassword/logging/uptime-kuma. `backup/config.json.tmpl` checked in with a single repo (`podhaus` at `/repos/podhaus`, password from 1P) and 9 plans — one per stack with state — with schedules staggered 02:00–03:20 AWST, `14 daily + 4 weekly + 6 monthly` retention on every plan, weekly prune (Sun 04:00) and check (Sun 05:00) on the repo. The `backrest-state` plan (runs last in the staggered schedule) has a post-snapshot success hook that fires the nightly `rclone sync` to OneDrive once. komodo-start renders the template via `envsubst` from the 1P-stored restic password into `/etc/komodo/backup/config.json`; the compose bind-mounts `/etc/komodo/backup` writably so Backrest can do its startup rewrite + UI edits can persist in-memory, but any drift is erased on the next komodo-start run. Auth is disabled (LAN-only access, no backrest tunnel ingress). First manual snapshots taken of every plan: 14.733 GiB of source data across 9 snapshots, 12 GB on-disk in the Pouch repo. Restore drill verified byte-for-byte against `.rtorrent.rc` from the flood plan. Commits `d09afc9`.
- [x] **Fix read-only rclone.conf mount** — done 2026-04-16 as part of the Backrest config-as-code work. Symptom: `docker exec backrest rclone lsd onedrive:Backups` returned the listing but first emitted `ERROR: Failed to save config … rename /rclone/rclone.conf /rclone/rclone.conf.old…: device or resource busy` — same bind-mount-over-a-file class of failure as Backrest's own config.json rewrite issue. Every rclone call was triggering a refresh, couldn't persist the new token, and would eventually have broken the nightly sync when the in-memory refresh token expired or rate-limited. Fix: changed `backup/compose.yaml` from `/etc/komodo/rclone/rclone.conf:/rclone/rclone.conf:ro` to a writable directory bind mount `/etc/komodo/rclone:/rclone:z`, which lets rclone rename-write its config in place. Drift story is the same as the Backrest config: komodo-start re-renders `rclone.conf` from the 1P Secure Note on next bootstrap. Post-fix `rclone lsd` returns clean with no ERROR.
- [x] **ofelia stack + Plex stats cleanup** — done 2026-04-16. `ofelia/compose.yaml` + `stack.toml` deployed (`mcuadros/ofelia:latest`, daemon mode + `--docker` for label discovery, RO docker socket, `security_opt: label:disable` for SELinux, no central config). `plex/stats-cleanup.sh` does an online DELETE of `statistics_bandwidth` rows older than 30 days via Plex's bundled SQLite (handles the icu_root collation), then a VACUUM to reclaim freed pages. Verified end-to-end: rows 3709→3311 (-398), oldest 2026-01-01→2026-03-17, VACUUM took 2 s and reclaimed 9.28 MB (232 MB→223 MB). Labels on `plex/compose.yaml` schedule it for `0 4 1 * *` AWST — 04:00 on the 1st of each month, after the nightly Backrest pipeline finishes (~03:30 incl. rclone sync) so today's snapshot preserves the pre-cleanup state. **Discovered**: ofelia exits fatally if the scheduler has zero jobs at startup (rather than waiting for label-bearing containers to appear later). The chicken-and-egg means ofelia has to be deployed AFTER target containers have ofelia.* labels, OR you have to restart ofelia after the labels exist. Worked around by restarting ofelia once the plex container picked up its new labels. Commit `858ae8a`.
- [ ] **Swap Loki for Victoria Logs** — Loki's time-only retention + hard-stop-on-full behaviour is a poor fit for home-lab resource bounds. Victoria Logs has native size-based retention, ~7× less disk for equivalent logs, and speaks the Loki push API natively. Alloy and Grafana don't change; only the storage/query layer does. Steps:
  - Update `logging/compose.yaml`: replace `loki` with `victoria-logs` (image `victoriametrics/victoria-logs:latest`, port 9428, volume `/var/lib/victoria-logs-data`, args `-retentionPeriod=31d -retention.maxDiskUsageBytes=50GB`).
  - Update Alloy config to push to `http://victoria-logs:9428/insert/loki/api/v1/push` (drop-in).
  - Install the Victoria Logs datasource plugin in Grafana, configure, remove Loki datasource.
  - Delete `loki-config.yaml`.
  - Known limitation to accept: LogQL metric-from-log extraction is more mature than LogsQL's. We don't currently use this. If we ever want alerting on log-derived metrics, path is `vmalert`. Filing as known, not blocker.
- [ ] **Seed Grafana dashboards** — "all container logs" with a `{host="bilby"}` panel, LogsQL syntax. Do after the Victoria Logs swap.
- [ ] **Verify `/Jump/plex` safety window expired** — 3-5 day wait after 2026-04-13 before deleting the original. After validation, `rm -rf /mnt/jump/plex` and `docker volume rm plex_jump` (no longer needed once Backrest compose is updated).
- [x] **Plex `/transcode` fix** — done 2026-04-16. Original plan was to fix the tmpfs permissions, but reconsidered the architecture: the tmpfs choice was historical baggage from "all bilby state on NFS" (RAM was the only safe place when disk meant NFS). Post-phase-6 the M1 NVMe handles transcode's ~1 MB/s of writes trivially, and freeing 4 GB RAM headroom is more valuable than the theoretical RAM speed advantage. Replaced `tmpfs: - /transcode:size=4g` with bind mount `/var/lib/plex-transcode:/transcode:z`, host dir owned `1000(nathan):100(users) 755` to match Plex's verified runtime uid/gid (PLEX_UID=1000 PLEX_GID=100). Container restart took 8 seconds, post-restart verified Plex was writing to `/var/lib/plex-transcode/Transcode/Sessions/` (19 MB accumulated within minutes from active viewing) with zero `IsDirWritable` errors. Commit `6683194`.

### Phase 7: Stack migrations

For each stack: take a pre-migration snapshot on alligator → stop on alligator → rsync volume(s) to the new home (local or Pouch per sizing rule) → deploy on bilby via Komodo → verify → add healthcheck + autoheal label → confirm Backrest plan exists and captures the new state → add Kuma monitors for the new container.

- [x] **flood** — done 2026-04-14. rsynced `flood_flood-db` from alligator (208 torrents, ~13.7 MB of rtorrent session files) → local bind at `/var/lib/flood-db`. Compose volume swapped NFS → local, `:z` SELinux flag added, `MEDIA_DIR=[[MEDIA_DIR]]` + `TZ=[[TZ]]` added to `flood/stack.toml` environment, `TZ: ${TZ}` added to compose env. rtorrent loaded all 208 torrents from the rsynced session without rehashing. Commits `46a0113`, `8d458d8`, `336b961`.
- [x] **home-assistant** — done 2026-04-14. rsynced `home-assistant_home-assistant-config` from alligator (2,481 files, ~81 MB, owned root:root inside the container) → local bind at `/var/lib/home-assistant`. Stopped HA on alligator first so SQLite could checkpoint its WAL into the main DB cleanly. Compose simplified: removed the NFS volume block, direct bind mount `/var/lib/home-assistant:/config:z`. `network_mode: host`, `privileged: true`, and the `/run/dbus` mount unchanged. HA came up healthy in 7s, custom integrations (`hacs`, `victorsmartkill`) loaded successfully, `https://home.pod.haus` returns 302 (Cloudflare Access login). Post-migration HA cleanup done in the same session: stale Bluetooth config entry deleted (no dependent devices), Grove Sonos device removed (Play:5 with stuck firmware-level alarm cache state — see progress log), HA admin password reset (drift between password manager + on-disk hash). Commit `19e60ac`.
- [x] **paperless** — done 2026-04-14. Cleaned up the four `paperless-*` containers stuck in `Created` from a never-completed prior deploy attempt + their two empty docker volumes. Verified `POSTGRES_PASSWORD` matches between alligator's running container and the Komodo-stored variable (precondition for migrating pgdata). Stopped paperless on alligator gracefully (app + dependencies first, postgres last with `--time 60` to allow clean WAL flush). Rsynced 68 MB pgdata + 16 MB paperless-data to local binds at `/var/lib/paperless-{pgdata,data}`. Caught and fixed the rsync gid drift (rsync's default name-based ownership preservation translated `gid 999 (docker on alligator)` to `gid 986 (docker on bilby)` instead of preserving numeric `999`; `chown -R 999:999` corrected it). Created `/mnt/pouch/Paperless/` on the NAS (the documents media bind mount target was missing). Compose updated: dropped the NFS data volume + the unused docker volume block, bind-mounted `/var/lib/paperless-pgdata:/var/lib/postgresql/data:Z` and `/var/lib/paperless-data:/usr/src/paperless/data:z`. **Discovered another distroless healthcheck bug**: `apache/tika:latest` has no shell/wget/curl, so the in-container `wget` healthcheck could never pass. Fixed by dropping the healthcheck and autoheal label from `paperless-tika` (same shape as `cloudflare-tunnel`'s fix). All 5 containers healthy on bilby, postgres came up cleanly with the migrated data (3 auth_user rows preserved, 0 documents/tags/correspondents matches alligator's empty state), `https://paperless.pod.haus` returns 302 to the login page externally. Commits `d4c66ac` (compose swap) + `68ae683` (tika healthcheck fix).

### Phase 9: Finish OneNote export on bilby

Using `hkevin01/onenote-exporter` via Microsoft Graph API. Output lives at `<NFS>/Nathan/Notes Export Graph API/`. As of the last survey, 668 / 1,111 pages exported (~1.2 GB on NFS). Remaining: ~442 pages of Life notebook plus 1 stray Financial page. Exporter is incremental (SQLite catalog) so resuming is safe.

- [ ] Clone `hkevin01/onenote-exporter` fresh on bilby
- [ ] Copy SQLite catalog from alligator: `~/repos/onenote-exporter/cache/db/catalog.sqlite`
- [ ] Copy token cache (or re-auth via device code flow)
- [ ] Build exporter image on bilby
- [ ] Run `./paperless/export-remaining.sh`
- [ ] Verify final page counts match plan

### Phase 10: Paperless upload (Phase 3 of onenote-to-paperless.md)

- [ ] Write upload script — walks export tree, uploads via `POST /api/documents/post_document/`, tags by notebook + section
- [ ] Dry-run against one small notebook (Shadow? Travel?)
- [ ] Full import
- [ ] Verify in Paperless UI: tag counts, sample search, spot-check attachments

### Phase 11: Railway migrations — doggos and yiayia

Two additional Railway-hosted services that need to come local. Services unknown — survey first, plan second, execute third. Done after Cloudflare tunnel cutover so local tunnel routing is stable before reshuffling DNS.

- [ ] Survey Railway projects via `railway` CLI: image, env vars, volumes, exposed ports, persistent state
- [ ] Document what each service is (`doggos.indigo.pod.haus` and `yiayia.pod.haus`) — purpose, dependencies, data sensitivity
- [ ] Create `doggos/` and `yiayia/` directories in the repo with `compose.yaml` + `stack.toml`
- [ ] Migrate state via `sqlite3 .backup` or `tar` of volume contents via Railway CLI
- [ ] Deploy local stacks via Komodo
- [ ] Add ingress rules to `cloudflare-tunnel/compose.yaml`
- [ ] Update `dns/dnsconfig.js`: remove Railway CNAMEs, let the tunnel handle both
- [ ] `dns-push`, verify
- [ ] Add Kuma monitors for both
- [ ] Decommission Railway projects (keep 72h for rollback, then delete)

### Phase 12: Retire alligator — done 2026-04-14

- [x] All `*.pod.haus` routes verified on bilby (phase 7 completion)
- [x] Alligator's containers gracefully stopped during phase 7 migrations
- [x] Alligator powered off by user after OneNote catalog + `dns/.env` extracted
- [x] `A("alligator", "10.0.0.83")` removed from the UniFi zone, dns-push applied (commit `399ad7f`)
- [ ] Syncthing relocation to NAS host — **deferred**, separate work from the podhaus migration. Tracked in Deferred section below.

### Phase 13: Rewrite README (last step, after everything is done)

Write the README from as-built reality, not from intent. Deferred to the end so it reflects what was actually built. The current README is stale — still describes the pre-Komodo `run` script architecture with only a small NAS storage section added during this migration.

- [ ] Audit what's actually running on bilby and what lives in the repo
- [ ] Rewrite top-level service list to match reality
- [ ] Remove the Komodo "migration in progress" framing — the migration is done
- [ ] Document the current bootstrap story (`komodo-start`, 1Password → komodo-op → Variables, stack conventions)
- [ ] Add a storage/backup/recovery overview
- [ ] Add a disaster-recovery runbook paragraph

## Credentials

All four credentials live in 1P Homelab and surface as Komodo Variables via komodo-op.

| Item | 1Password field | Purpose |
|---|---|---|
| `railway-api-token` | `credential` | Scripted access to Railway projects (Kuma, doggos, yiayia) |
| `postmark-smtp` | `username`, `credential`, `server`, `port` | Backup + alert notifications via SMTP |
| `restic-repo-password` | `credential` (48-char base64, ~288 bits) | Restic repository encryption. Annotate the 1P Emergency Kit hardcopy with a note pointing to this item as part of the migration wrap-up. |
| `rclone-onedrive-token` | `notesPlain` (Secure Note) | rclone OneDrive backend for off-site restic sync. Full `[onedrive]` config block including token + drive_id. Re-provisioning runbook: see commit history / README — browser OAuth dance on a laptop, paste full `rclone config show onedrive` output into the Secure Note. |

## Deferred (non-blocking follow-ups)

- **Plex metadata regeneration validation** — future experiment. If plex cache/metadata turns out to be reliably regenerateable, add restic exclusion rules and reclaim ~100 GB of repo space. Not a migration blocker.
- **Syncthing relocation to NAS host** — separate work after alligator is retired.
- **Grafana admin password rotation + dashboard seeding** — Grafana deploys with default `admin/admin` credentials. Rotate on first login. Dashboards deferred until after the Victoria Logs swap.
- **`komodo-start` first-boot API key dance** — Currently the script assumes a pre-existing `Komodo API OnePassword Sync` item in 1P that matches the running Komodo DB. On a fresh install this is false; manual workaround is in the progress log. Fold into `komodo-start` as an automatic first-boot path: try the existing key, if the API call 401s, fall back to admin login + mint + `op item edit`, then proceed.
- **Upstream `ghcr.io/0dragosh/komodo-op` Dockerfile PR** — upstream Dockerfile hardcodes `--platform=linux/amd64` + `GOARCH=amd64`, making the multi-arch manifest's arm64 tag a mislabelled amd64 image. Our local workaround is `onepassword/komodo-op.Dockerfile`. Upstream fix is buildx `BUILDPLATFORM`/`TARGETPLATFORM` args. Low priority but polite.
- **Plex `Preferences.xml` templating** — check `plex/Preferences.xml` into the repo as source of truth, with `PlexOnlineToken` as an `op://Homelab/plex-online-token/credential` reference. `plex-init` container in `plex/compose.yaml` that renders the template via envsubst on every boot, enforces a narrow allow-list of critical attrs (`MachineIdentifier`, `ProcessedMachineIdentifier`, `CertificateUUID`, `AnonymousMachineIdentifier`, `PublishServerOnPlexOnlineKey=1`, `ManualPortMappingMode=1`, `ManualPortMappingPort=32400`, `AcceptedEULA`, `IPNetworkType`, `TranscoderTempDirectory`, `DlnaEnabled`, `LanguageInCloud`), and leaves runtime-mutated attrs alone. **Rationale:** Plex identity is a magic string that's catastrophic to lose on a rebuild — template in git turns a multi-hour recovery into `git clone` + deploy. Originated from an April 2026 remote-access debugging session that found `PublishServerOnPlexOnlineKey="0"` silently off; those attrs live only in the container volume now, which is exactly the fragility config-as-code should eliminate.

## Progress log

Chronological diary. Pre-2026-04-12 entries compressed into the first bullet below. Entries since the phase 6 architecture pivot kept at higher fidelity because they still inform ongoing work.

- **Pre-2026-04-12: Planning and bootstrap.** Document created from a full survey of alligator. Architecture decisions locked in incrementally: backup approach (restic → Pouch → rclone → OneDrive), layered failure coverage, logging solution (Loki+Alloy+Grafana, now being swapped to Victoria Logs), autoheal inclusion list, restic repo layout + encryption, validate-and-maybe-restore backup pattern, Postmark SMTP notifications. Credentials all staged into 1P (`railway-api-token`, `postmark-smtp`, `restic-repo-password`, `rclone-onedrive-token`). rclone OneDrive provisioning runbook executed (picked OneDrive Personal, drive_id `397B8A27385EB8E3`). Uptime Kuma and `doggos`/`yiayia` added as Railway migration phases. Plex remote-access outage debugged out-of-band and fixed (`PublishServerOnPlexOnlineKey="0"` root cause); follow-up added to template `Preferences.xml` into the repo. Config-as-code adopted as a first principle for the repo. **Phase 1 pre-flight cleanup complete** (commit `d2d7346`). **Phase 2 Komodo bootstrap complete** — komodo-core/postgres/ferretdb/periphery, onepassword stack, autoheal all healthy; 28 Komodo Variables populated. Env-specific workarounds (NAS squash carve-outs, SELinux `label:disable`, komodo-op local arm64 build, Komodo first-boot key dance, multi-line secret rendering to `/etc/komodo/rclone/rclone.conf`, plex-backup retirement due to 172.18.0.0/16 network squat, absolute host paths for compose bind mounts) discovered and worked around. **Phase 3 backup stack complete** (restic repo `857078229d`, first snapshot `866cbd66` of `/Jump/plex`, restore drill successful, rclone sync to OneDrive at 17.526 GiB / 19m14s — backup gate satisfied). **Phase 4 logging stack complete** (Loki + Alloy + Grafana, logs flowing). **Phase 5a uptime-kuma scaffolding complete**. Alligator deliberately untouched throughout — every bilby stack runs in parallel with its alligator counterpart until the phase 7 + 8 cutover.

- **2026-04-12/13: Phase 6 architecture pivot.** A Plex client-visible outage (occasional 30-60s stalls during browsing) led to a multi-day debugging session that uncovered layered problems: wrong host timezone (butler running at peak hours), missing theme-music DB records (triggered inline analysis under lock), and the real killer — `statistics_bandwidth` had 796,867,671 rows (9 years of history, ~55 GB of the 47 GB DB file) with no Plex-provided pruning. Attempted in-order: host TZ fix, multi-hour online VACUUM (killed by autoheal after ~7h), force refresh (damaged user poster selections), then the actual fix: offline table rebuild keeping only last 30 days (796M → 3,405 rows), VACUUM completed in ~60s, DB 47 GB → 217 MB. Rebuilt Plex from a fresh `rsync /Jump/plex` onto local NVMe at `/var/lib/plex-local`. BIFs (183 GB at `/mnt/pouch/plex-video-thumbnails`) switched from an in-config symlink to an explicit bind mount in compose. **Design principle changed from "stateless bilby on NFS" to "local state is the default."** Sizing rule locked in: local <5 GB, consult 5-50 GB, Pouch ≥50 GB. NAS squash carve-outs became permanent design choices rather than workarounds.

- **2026-04-13: Cleanup + remaining plan locked in.** Deleted 111 GB of Plex test copies from `/Jump` (`plex-sync`, `plex-clean`, `plex-local-meta`, `plex-test`). Deleted the April 11 restic snapshot via `restic forget 866cbd66 --prune` (17.5 GiB reclaimed locally), then `rclone sync`'d the empty repo to OneDrive to clean the mirror (1,103 files / 17.5 GiB deleted remotely). Backrest has no active plans so no backups are running until we write `backup/config.json.tmpl`. Paperless deferred until flood + HA + tunnel cutover. Phase 6.5 "Post-recovery follow-ups" added to the plan.

- **2026-04-13: Loki → Victoria Logs decision.** Loki's time-only retention + hard-stop-on-full behaviour is a poor fit for "logging must not break the rest of the infrastructure." Victoria Logs has native size-based retention, ~7× less disk for equivalent logs, and speaks the Loki push API natively so Alloy needs no changes. Zero dashboards built yet, so migration cost is essentially zero. Phase 4 updated; phase 6.5 holds the swap item. Known feature gap (LogsQL metric-from-log extraction less mature than LogQL) accepted as non-blocking.

- **2026-04-13: Ofelia chosen for recurring jobs.** Evaluated host systemd timers, Komodo Procedures, Ofelia, Dockron, in-container cron. Ofelia wins because it's Docker-native (label-based mode colocates the schedule with the service), no new templating. Plex stats cleanup becomes an Ofelia stack + labels on `plex/compose.yaml` + `plex/stats-cleanup.sh` script. Backrest retains its own scheduler.

- **2026-04-14: Flood migrated + tunnel cutover executed in a single session.** Alligator was briefly offline earlier in the day (tunnels returning 530 from both hosts); user confirmed no coordination needed. When alligator came back mid-session, the phase 8 "hot swap" reduced to a plain sequential cutover: stop alligator's flood → rsync flood state → start bilby's flood → stop alligator's cloudflare-tunnel → start bilby's cloudflare-tunnel. rtorrent state (208 torrents, ~13.7 MB of `.torrent`/`.libtorrent_resume`/`.rtorrent` files) rsynced from alligator's `/var/lib/docker/volumes/flood_flood-db/_data/` to bilby's `/var/lib/flood-db/` over SSH with sudo-rsync. The `-X` xattr-preservation flag produced harmless SELinux `lremovexattr` warnings. rtorrent loaded all 208 torrents without rehashing, meaning the `.libtorrent_resume` bitfields survived the transfer. Commits `46a0113` (cutover), `8d458d8` (MEDIA_DIR stack.toml reference), `3bba849` (tunnel healthcheck removal).

- **2026-04-14: Komodo variable seeding drift discovered and fixed.** Deploying flood on bilby failed at `docker compose config` with `The "MEDIA_DIR" variable is not set`. Investigation: `MEDIA_DIR` and `TZ` are declared in `komodo/sync/variables.toml` and ResourceSync reads the file, but the sync's `variable_updates` output is empty — ResourceSync reads variable TOMLs but doesn't apply them. Audit found three stacks (`logging`, `uptime-kuma`, `backup`) with on-disk `.env` files containing `TZ=[[TZ]]` literal, though running containers still had correct `TZ=Australia/Perth` from original deploys because docker-compose hadn't recreated them. Next redeploy of any affected stack would silently fall back to UTC — exactly the bug that caused the Plex butler incident. **Fix landed in commit `336b961`**: `komodo-start` now calls `seed_variable TZ false` and `seed_variable MEDIA_DIR false` alongside the 1P credentials, with `is_secret` as a new `seed_variable` parameter. Affected stacks redeployed via Komodo API to rewrite their `.env` files — no container recreation needed because compose specs were unchanged. `flood/stack.toml` and `flood/compose.yaml` backfilled with TZ references at the same time.

- **2026-04-14: Cloudflare-tunnel healthcheck was structurally impossible.** `cloudflare/cloudflared:latest` is distroless — no shell, no wget, no curl, only the `cloudflared` binary. The original `healthcheck: wget http://localhost:20241/ready` could never actually run. Previous deploys presumably relied on the health state staying `starting` indefinitely. Removed the healthcheck and autoheal label (commit `3bba849`); cloudflared handles its own reconnection, `restart: unless-stopped` catches crashes, external liveness will be monitored via Uptime Kuma hitting the public hostnames.

- **2026-04-14: Plex `/transcode` tmpfs is not writable by Plex.** Found investigating a potential TV app crash report. `tmpfs: - /transcode:size=4g` mounts as `root:root 755`, but Plex runs as uid 1000. Log shows `IsDirWritable: directory '/transcode' is not writable` on every transcode attempt. Video direct-play works fine (doesn't hit `/transcode`) but theme music always transcodes and always fails. Has been broken since plex came up on bilby. Fix is a one-line compose change (`mode=1777` tmpfs option) but requires container restart — deferred to next maintenance window. Filed as phase 6.5 item.

- **2026-04-16: Plex /transcode → local NVMe bind, not just a permissions fix.** When the time came to address the deferred /transcode item, user pushed back on the "tmpfs in RAM" choice itself: "I am dubious about the plan to put a transcode tmpfs in ram, it feels like a waste. Now that we've moved to local storage I see no reason our very high speed ssd is an issue for it." Agreed — the tmpfs choice was historical baggage from "all bilby state on NFS" (back when disk meant NFS, RAM was the only safe target for transcode scratch). Post-phase-6 M1 NVMe sustains 1+ GB/s vs transcoding's ~1 MB/s — RAM "speed advantage" is theoretical not practical, and freeing 4 GB of RAM on a 15 GB host running komodo-postgres + paperless-postgres + plex + everything else is meaningful headroom. Replaced `tmpfs: - /transcode:size=4g` with bind mount `/var/lib/plex-transcode:/transcode:z`. Verified Plex's actual runtime uid/gid before chowning (`/proc/<pms_pid>/status` showed `Uid: 1000` `Gid: 100` matching PLEX_UID/PLEX_GID env), set host dir to `1000(nathan):100(users) 755`. Recreated plex container, came back healthy in 8 seconds. Verified `/var/lib/plex-transcode/Transcode/Sessions/` accumulated 19 MB within minutes of restart from active viewing, zero `IsDirWritable` errors in post-restart logs. Bonus: solves the original permissions bug for free since the bind mount has correct ownership built in. Commit `6683194`.

- **2026-04-14: Migration doc cleaned up.** Completed phases collapsed to one-paragraph summaries; blow-by-blow detail preserved only where it still informs active work. Resumption pointer, Environment-specific workarounds, Known gotchas, and Phase 6.5 + 7 active work kept at full fidelity. Progress log pre-2026-04-12 entries collapsed into a single summary bullet. Target: keep the doc usable as both a task list and a decision log without it ballooning as work continues.

- **2026-04-14: home-assistant migrated to bilby.** Inspected alligator's `home-assistant_home-assistant-config` volume — 85 MB, 2,519 files, owned root:root (HA image runs as root by default), with an active `home-assistant_v2.db` + WAL. Stopped HA on alligator (exit 137 after the 10s graceful window — HA didn't cleanly flush in time, but SQLite's WAL mode + crash safety guarantees the on-disk state is still consistent; the main DB file grew by ~61 KB during shutdown, confirming the WAL was checkpointed into it). Rsynced 2,481 files / 79 MB over SSH with sudo-rsync, no errors. Updated `home-assistant/compose.yaml`: removed the entire NFS volume declaration, swapped the bind to `/var/lib/home-assistant:/config:z` directly. `stack.toml` already referenced `[[TZ]]` which works now that the variable-seeding fix has landed. Deployed via Komodo API; HA came up healthy in 7 seconds (healthcheck passes because wget follows the 302 redirect on `/` to a 200 response). `https://home.pod.haus` externally returns a Cloudflare Access 302 (previously 530/502 during the window between tunnel cutover and HA landing). Found three Sonos-related log entries on first startup: two "Subscription to \<ip\> failed, attempting to poll directly" for speakers at `10.0.0.63` and `10.0.0.59` (expected — Sonos speakers cache UPnP callback URLs from HA's old IP on alligator and need to re-subscribe; HA transparently polls in the interim), and one `SonosUpdateError: Alarm list UID RINCON_B8E937F8C9D401400:0 does not match RINCON_B8E937F8C5F001400:0` for the "Grove" speaker — two different MAC prefixes so this looks like a hardware replacement at some prior point that left a stale reference in HA's device registry. Not caused by the migration but worth investigating later. Custom integrations (`hacs`, `victorsmartkill`) loaded fine. Commit `19e60ac`.

- **2026-04-14: HA cleanup session — Grove Sonos saga + post-migration tidying.** User couldn't log in to HA after the migration; the on-disk bcrypt hash in `auth_provider.homeassistant` was stale (mtime Oct 2024). Reset the password by computing a new bcrypt+base64 hash inside the HA container's Python (round-trip verified before writing), edited the JSON store directly, restarted HA, verified end-to-end with `POST /auth/login_flow`. Then deleted the stale Bluetooth config entry (Intel adapter MAC `04:EA:56:DC:56:44` from alligator's hardware, 0 dependent devices) via the REST `DELETE /api/config/config_entries/entry/<id>`. Then the Sonos rabbit hole: tried reloading the Sonos integration via REST to clear UPnP subscription state (partial success — different speakers ended up wedged after the reload than before, indicating shifting per-speaker problems). Investigated the Grove `SonosUpdateError` deeply: queried each speaker's `alarmClock.ListAlarms()` directly via SoCo and discovered Grove was the only speaker reporting the OLD RINCON `B8E937F8C9D401400:0` while every other speaker had already updated to the current Grove's UID `B8E937F8C5F001400:0`. Tried nudging Grove via a SoCo `CreateAlarm` + `DestroyAlarm` round-trip on Studio (other speakers picked up the new version within 2s, **Grove never received the event**). Discovered the SonosUpdateError has a deeper consequence — HA's `_add_speaker` only catches `(OSError, SoCoException, Timeout)` but `SonosUpdateError` extends `HomeAssistantError` and escapes that catch, breaking the entire `_add_speakers` loop and leaving all 6 speakers `unavailable`. **Bug class: HA's per-speaker exception is too narrow** (worth filing upstream eventually). User physically power-cycled Grove → Grove's persistent flash state was unchanged, alarm UID was still stale post-reboot, confirming this is firmware-level corruption that survives reboots. Reconciled the full Sonos device list against physical reality: Studio (Playbar), Garage (Sonos One), Ladybug (SYMFONISK Bookshelf, daughter's room), Burrow (Sonos One, hidden in user's bedroom), Outside (Sonos Move, charging), Grove (Play:5, "essentially never used" so disconnected for good). User physically unplugged Grove and asked for it to be removed from HA entirely. Removed Grove from device_registry via WebSocket `config/device_registry/remove_config_entry`, restarted HA. With Grove no longer discoverable via SSDP, the integration setup completed cleanly and all 4 reachable speakers came up `idle`. Outside stays `unavailable` until the Move is re-charged. **Process discovery during this session**: I had also patched `/usr/src/homeassistant/homeassistant/components/sonos/__init__.py` to add `HomeAssistantError` to the except clause — the user explicitly rejected this as "a terribly unmaintainable shitshow fix" and instructed: "patches only ever happen at my explicit consent". Reverted the patch in place (file is now byte-for-byte upstream verbatim) and saved a `feedback_no_patches.md` memory to enforce the rule going forward. Also saved `feedback_sonos_app.md` after the user noted their S1-era hardware isn't compatible with the current Sonos S2 controller app. Final HA health sweep: 4 of 5 reachable Sonos speakers `idle`, Nanoleaf Shapes 1114 unreachable (stale ISP-delegated IPv6 prefix in HA's stored host — pre-existing and the user has unplugged it anyway), 2 of 6 BAF Haiku fans physically off (normal household state), 20 of 47 Hue lights unavailable (normal — wall switches off). HA called done.

- **2026-04-14: Alligator retired — Phase 12 complete.** Pre-shutdown sweep before powering alligator off: grabbed the OneNote exporter SQLite catalog (`~/repos/onenote-exporter/cache/db/catalog.sqlite`, 532 KB) to `/home/nathan/repos/podhaus-migration-state/onenote-exporter/cache/db/` on bilby per phase 9 prerequisites, and pulled the gitignored `dns/.env` (op:// env var declarations for CLOUDFLARE_API_TOKEN + UNIFI_API_KEY) which was only on alligator. Removed `A("alligator", "10.0.0.83")` from the UniFi zone in `dns/dnsconfig.js`, ran `dns-push` (hit a SELinux bind mount issue on the first try — the `ghcr.io/stackexchange/dnscontrol` container couldn't read the host `dns/` dir because the volume mount lacked `:z`, fixed `dns-preview` and `dns-push` scripts to include `:z`). dnscontrol applied the `DELETE alligator.pod.haus` correction successfully. User powered off alligator. Remaining items on alligator flagged and surfaced but not grabbed: `~/.config/syncthing/` (5.1 GB — deferred to syncthing-to-NAS relocation), `~/syncthing-backup-20260301.tar.gz` (802 MB), uncommitted changes in several non-podhaus repos (clusterized-snapshots, mine-simulation, switch-technologies terraform, onenote-exporter local changes), various dotfiles. User chose to accept that state remains only on alligator's disk. Commit `399ad7f`.

- **2026-04-16: Backrest config-as-code deployed — backup gap closed.** Wrote `backup/config.json.tmpl` with 1 repo + 9 plans + 14d/4w/6m retention + weekly prune/check + rclone sync hook. Rewritten `backup/compose.yaml`: dropped dead `plex_jump` external volume, mounted all migrated stack state read-only at `/userdata/<stack>`, added `external: true` refs for docker named volumes. Discovered Backrest's `config version 0 is invalid` startup error → added explicit `"version": 4` to the template. Discovered Backrest tries to rewrite config.json in place via rename-based atomic write, which is incompatible with a read-only file bind mount (returns "device or resource busy") → changed from a file bind mount to a writable directory bind mount at `/etc/komodo/backup:/config:z`. Drift story: any UI edits or Backrest's own normalization persist on-disk until the next `komodo-start` run re-renders the template from 1P. Auth disabled (LAN-only, no tunnel ingress for Backrest). `komodo-start` gained a render step: `envsubst RESTIC_REPO_PASSWORD` from 1P into the template, written to `/etc/komodo/backup/config.json`. Also fixed the rclone.conf read-only mount in the same pass — same class of bind-mount-over-a-file failure was trapping rclone in a permanently-refreshing-token-but-can't-persist loop; changed `/etc/komodo/rclone/rclone.conf:ro` file mount to `/etc/komodo/rclone:/rclone:z` directory mount. Post-deploy: all 9 plans scheduled correctly (02:00–03:20 AWST daily), manually triggered all 9, snapshots landed (14.733 GiB of source, 12 GB on-disk). Restore drill: `restic restore flood → diff .rtorrent.rc` passed byte-for-byte. rclone sync to OneDrive initiated. Cleaned up orphan volumes: `backup_backrest-config`, `backup_backrest-rclone`, `home-assistant_home-assistant-config`. Commit `d09afc9`.

- **2026-04-14: paperless migrated to bilby — Phase 7 complete.** User context: "none of our state in paperless currently matters, we started setting up, started migrating, didn't finish. Nothing exists that isn't also in OneNote still." Inspected alligator: 5 containers running healthy, postgres 16.13, paperless 2.20.10, **0 documents in DB**, 68 MB pgdata + 17 MB paperless-data + ~16 KB unused media volume. Verified `POSTGRES_PASSWORD` env on alligator's running container matched the Komodo-stored variable byte-for-byte (precondition for migrating pgdata across without postgres refusing to start). Cleaned up the four `paperless-*` containers stuck in `Created` state on bilby plus their two empty docker volumes (paperless was the second-pass cleanup of the half-completed prior deploy attempt). Stopped paperless on alligator gracefully — app + Tika + Gotenberg + Redis first (`--time 30`), then postgres last (`--time 60`) for clean WAL flush. All exited 0 except tika which got SIGKILL'd at 143. Rsynced both volumes via sudo-rsync over SSH. **Caught the rsync gid drift gotcha**: rsync's default name-based ownership preservation translated `gid 999 (docker on alligator)` to `gid 986 (docker on bilby)` instead of preserving numeric `999`. Fixed in place with `chown -R 999:999` rather than re-running rsync with `--numeric-ids`. Postgres mode-700 means group perms don't apply so the wrong gid wouldn't actually have broken postgres, but the principle of correct-by-construction was worth the second. Found `/mnt/pouch/Paperless/` was missing on the NAS (the documents bind mount target had never been created), so created it. Updated compose: dropped the NFS data volume + named docker volume block, bind-mounted `/var/lib/paperless-{pgdata,data}` directly. **Discovered another distroless healthcheck bug** — same shape as cloudflare-tunnel: `apache/tika:latest` has no shell/wget/curl in the image, so the existing `wget` healthcheck could never have run. Dropped the healthcheck + autoheal label from paperless-tika (same fix shape as cloudflare-tunnel from earlier today). All 5 containers came up cleanly: paperless app healthy, postgres healthy with the migrated DB (3 auth_user rows including the `nathan` admin, 0 documents matches the original empty state), redis + gotenberg healthy, tika running with no health label. `https://paperless.pod.haus` externally returns 302 to login. Commits `d4c66ac` (compose volume swap) + `68ae683` (tika healthcheck removal). **Phase 7 fully complete — every Komodo-managed stack now lives on bilby.**

- **2026-04-16: Ofelia + Plex stats cleanup live.** Added `ofelia/` stack (`mcuadros/ofelia:latest`, daemon mode + `--docker` for label discovery, RO docker socket, `security_opt: label:disable`, no central config). Wrote `plex/stats-cleanup.sh` to do an online DELETE of `statistics_bandwidth` rows older than 30 days followed by VACUUM, all via Plex's bundled SQLite (handles the icu_root collation that system sqlite3 can't). User pushback during the work shifted it from the original weekly schedule to monthly: the table grows ~30 rows/day so even a missed month means ~900 extra rows of headroom, not gigabytes; weekly was over-cautious. Also adjusted the cron from 03:00 to 04:00 AWST so the cleanup sits AFTER the nightly Backrest pipeline (which finishes ~03:30 with the rclone OneDrive sync hook) — that way the daily snapshot preserves the pre-cleanup state. Added VACUUM as a second step after DELETE because the user explicitly asked for actual disk reclamation rather than just a row count drop. Verified end-to-end with a manual run: 3709→3311 rows, oldest 2026-01-01→2026-03-17, VACUUM took 2 s and reclaimed 9.28 MB (232→223 MB). **Discovered**: ofelia in daemon+label mode exits fatally if the scheduler has zero jobs at startup (logs "unable to start a empty scheduler" then dies → restart loop). Means ofelia must be deployed AFTER target containers have ofelia.* labels OR you have to restart ofelia after the labels exist. Worked around manually for this deploy; documented as a phase-6.5 post-mortem note. Restart of ofelia after plex labels existed picked up the job correctly: `scheduler.go:44 ▶ NOTICE New job registered "plex-stats-cleanup" - "/scripts/stats-cleanup.sh" - "0 4 1 * *"`. Commit `858ae8a`. Plex restart for the new bind mount + labels: container recreate ~7 s, came back healthy immediately.

- **2026-04-16: Host-installed restic + rclone on bilby (defense in depth).** During prep for the Plex restore drill, surfaced that we had no host-level restic/rclone — every backup operation was going through the `garethgeorge/backrest` container's bundled binaries. That made the entire recovery story implicitly depend on the backrest container being available (Docker Hub up, image pullable, container runtime working). User opted to install host binaries before the drill so the drill itself uses the more-realistic disaster-recovery tooling: `sudo dnf install restic rclone` on bilby gave us `restic 0.18.1` (matches container) and `rclone 1.73.0` (very close to container's 1.73.2). Both work with sudo against the existing `/etc/komodo/rclone/rclone.conf` (mode 600, root-owned) and the 1P-stored restic password. Now if the backrest container or its image vanishes, recovery is still possible via the host tools: `sudo restic -r rclone:onedrive:Backups/podhaus-restic snapshots` works directly. Follow-up worth filing: a "bilby host packages" runbook entry or list (for restic, rclone, op CLI, docker, anything else needed for a fresh-bilby rebuild) so reproduction is documented, not folkloric.

- **2026-04-16: Plex restore drill — restore mechanics validated, Plex.tv cloud sync confounded the validation.** Pretended `/mnt/pouch/backups` was lost; recovered Plex from the OneDrive copy. Methodology: installed host restic + rclone first (so the drill didn't depend on the backrest container being available), used host binaries throughout. Listed snapshots directly from OneDrive via `restic -r rclone:onedrive:Backups/podhaus-restic snapshots --tag plan:plex`, found `ef886826` (2026-04-16 09:02:25, 14.238 GiB) — confirms the snapshot is in OneDrive without trusting in-session memory. `rclone copy` of the 12 GB repo to local NVMe (rapid, NAS-bypass per user input that we're 2.5 Gbps WAN-side and would be NAS-write-limited otherwise). `restic restore ef886826 --target /var/lib/plex-restore-drill/plex-restored/` took 31 s on local NVMe to restore 97,185 files / 14.238 GiB. Ownership preserved as 1000:1000 via restic's numeric uid/gid (matches live state — gid=1000 vs Plex container's PLEX_GID=100 is harmless because owner perms cover Plex's access). Staged a one-off `/tmp/plex-restored-compose.yaml` identical to `plex/compose.yaml` except `container_name: plex-restored` and `/config` pointing at the restored data. Sequential cutover: `docker stop plex` (8.7 s graceful) → `docker compose -f /tmp/plex-restored-compose.yaml up -d` (1.7 s) → restored plex healthy in 9 s. Total downtime ~14 s. **Then the surprise**: user observed that watch state showed afternoon viewing (after the snapshot) — should have shown 09:02 state. Investigation: the restored DB was confirmed correctly restored (mount path, file mtimes preserved by restic, etc.) but the restored `metadata_item_settings` table had rows with `last_viewed_at` timestamps from after 09:02 — including rows from 21:12 (after cutover, from the user's testing). `ss -ntp` showed multiple connections from the restored Plex to 34.238.225.186:443 (plex.tv infrastructure). **Conclusion**: Plex Media Server with a valid Plex.tv token + the same MachineIdentifier syncs server state down from Plex.tv on startup. The snapshot was correctly restored byte-for-byte; Plex's cloud sync immediately overwrote the watch state with the more-recent account-side data after the container started. **The restore drill mechanics passed**: snapshot listed → repo downloaded → snapshot restored → container started cleanly → Plex came up serving the same library → cutover + revert both worked. **The validation observable was wrong**: watch state in the UI doesn't validate snapshot freshness when Plex.tv sync is active. For future drills, options are (a) firewall the restored container off plex.tv during validation, (b) mutate Preferences.xml to use a fresh MachineIdentifier so plex.tv treats it as a different server, (c) verify the snapshot via file checksums of metadata bundles that Plex doesn't touch on startup. User opted to call the drill a success on the mechanics and revert. Swap back: `docker stop plex-restored && docker start plex` — original container still on disk, original /var/lib/plex-local untouched, ~10 s total. Cleanup: removed plex-restored container, /tmp compose, /var/lib/plex-restore-drill (26 GB reclaimed). Live plex back on its original data with no observable user impact.
