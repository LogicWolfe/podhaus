[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]

[dev-packages]
pyyaml = "*"
# Control-node only. Nothing is installed on Ansible targets beyond the
# Python interpreter Fedora already ships. See docs/host-provisioning.md.
ansible-core = "*"
