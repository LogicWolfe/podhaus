# Shared service definition for autoheal (willfarrell/autoheal). Each
# host's compose.yaml in autoheal/<host>/ extends this via `include:`.
# The only thing that varies between hosts is bilby's SELinux label:disable
# (Fedora Asahi enforces; QTS doesn't), so kangaroo's overlay is just an
# include with no body.

services:
  autoheal:
    container_name: autoheal
    image: willfarrell/autoheal:latest
    restart: unless-stopped
    environment:
      AUTOHEAL_CONTAINER_LABEL: autoheal
      AUTOHEAL_INTERVAL: 30
      AUTOHEAL_START_PERIOD: 120
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    # No healthcheck — willfarrell/autoheal runs as a shell script with
    # no persistent process name to probe. restart: unless-stopped
    # handles crashes; autoheal can't heal itself anyway.
