# Bilby's autoheal overlay. Adds SELinux label:disable for Fedora Asahi,
# which denies cross-label writes to /var/run/docker.sock without it.


services:
  autoheal:
    security_opt:
      - label:disable
    labels:
      podhaus.stack-content-hash: ${STACK_CONTENT_HASH:-unset}
