configs:
  op-credentials:
    environment: OP_CREDENTIALS

services:
  op-connect-api:
    container_name: op-connect-api
    image: 1password/connect-api:latest
    restart: unless-stopped
    configs:
      - source: op-credentials
        target: /home/opuser/.op/1password-credentials.json
    volumes:
      - op-connect-data:/home/opuser/.op/data
    networks:
      - dockernet

  op-connect-sync:
    container_name: op-connect-sync
    image: 1password/connect-sync:latest
    restart: unless-stopped
    configs:
      - source: op-credentials
        target: /home/opuser/.op/1password-credentials.json
    volumes:
      - op-connect-data:/home/opuser/.op/data
    networks:
      - dockernet

  komodo-op:
    container_name: komodo-op
    image: ghcr.io/0dragosh/komodo-op:latest
    restart: unless-stopped
    depends_on:
      - op-connect-api
    networks:
      - dockernet
    environment:
      OP_CONNECT_HOST: http://op-connect-api:8080
      OP_VAULT: hjpenq2avoprqh2u3hqxap3jjq
      OP_SERVICE_ACCOUNT_TOKEN: ${OP_SERVICE_ACCOUNT_TOKEN}
      KOMODO_HOST: http://komodo-core:9120
      KOMODO_API_KEY: ${KOMODO_API_KEY}
      KOMODO_API_SECRET: ${KOMODO_API_SECRET}
      SYNC_INTERVAL: 1m
    labels:
      autoheal: "true"
    healthcheck:
      test: ["CMD-SHELL", "pgrep -x komodo-op > /dev/null || pgrep komodo-op > /dev/null"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 30s

networks:
  dockernet:
    external: true

volumes:
  op-connect-data:
    driver: local
    driver_opts:
      type: nfs
      o: "addr=10.0.0.25,nfsvers=4.1,soft,timeo=600,retrans=5,rw"
      device: ":/Jump/onepassword"
