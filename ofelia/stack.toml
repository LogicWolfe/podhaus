[[stack]]
name = "ofelia"
description = "Label-based scheduler for container jobs on bilby"
tags = ["podhaus"]

[stack.config]
server = "podhaus"
files_on_host = true
run_directory = "/etc/komodo/repo/ofelia"

environment = """
TZ=[[TZ]]
"""
