---
- name: Reload sshd for authorized keys
  # Reload, not restart: AuthorizedKeysFile is re-read per connection anyway,
  # and a restart would drop the very session applying this.
  ansible.builtin.systemd_service:
    name: sshd
    state: reloaded
